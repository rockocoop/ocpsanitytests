apiVersion: route.openshift.io/v1
kind: Route
metadata:
  annotations:
    openshift.io/host.generated: "true"
  labels:
    app: nginx
  name: nginx
  namespace: sanitytesting
spec:
  host: nginx-sanitytesting.{{ocpappsdomain}}
  to:
    kind: Service
    name: nginx
    weight: 100
  wildcardPolicy: None
